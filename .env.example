# Neon Postgres connection string, provisioned through the Vercel Marketplace
# `neon` integration. Required by `lib/db/index.ts` and `drizzle.config.ts`.
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require
